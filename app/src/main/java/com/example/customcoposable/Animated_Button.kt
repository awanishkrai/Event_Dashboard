package com.example.customcoposable

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.material3.Text

// This file has been cleared as per user request.
// The Event Dashboard is now in MainActivity.kt.

class Animated_Button : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            Text("Animated_Button Activity - Cleared")
        }
    }
}
